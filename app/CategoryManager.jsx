import { View, Text } from "react-native";

const CategoryManager = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text style={{ color: '#fff', fontSize: 24 }}>Category Manager</Text>
        </View>
    );
};

export default CategoryManager;